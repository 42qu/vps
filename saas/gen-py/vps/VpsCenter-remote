#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.8.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import VpsCenter
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  Vps info(i32 id)'
  print '  i32 to_open(i32 pc)'
  print '  void opened(i32 id)'
  print '  i32 to_close(i32 pc)'
  print '  void closed(i32 id)'
  print '  i32 netflow(i32 id, i64 bytes)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = VpsCenter.Client(protocol)
transport.open()

if cmd == 'info':
  if len(args) != 1:
    print 'info requires 1 args'
    sys.exit(1)
  pp.pprint(client.info(eval(args[0]),))

elif cmd == 'to_open':
  if len(args) != 1:
    print 'to_open requires 1 args'
    sys.exit(1)
  pp.pprint(client.to_open(eval(args[0]),))

elif cmd == 'opened':
  if len(args) != 1:
    print 'opened requires 1 args'
    sys.exit(1)
  pp.pprint(client.opened(eval(args[0]),))

elif cmd == 'to_close':
  if len(args) != 1:
    print 'to_close requires 1 args'
    sys.exit(1)
  pp.pprint(client.to_close(eval(args[0]),))

elif cmd == 'closed':
  if len(args) != 1:
    print 'closed requires 1 args'
    sys.exit(1)
  pp.pprint(client.closed(eval(args[0]),))

elif cmd == 'netflow':
  if len(args) != 2:
    print 'netflow requires 2 args'
    sys.exit(1)
  pp.pprint(client.netflow(eval(args[0]),eval(args[1]),))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
